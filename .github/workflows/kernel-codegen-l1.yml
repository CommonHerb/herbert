name: kernel-codegen-l1

# The far-axis silicon gate for the kernel-arc links. The native compiler's
# freestanding 32-bit Multiboot emit mode (selected by the "-- emit: multiboot32"
# directive) is graded NOT against the C reference but on a dual-substrate
# oracle: each emitted image must boot under BOTH QEMU (-kernel direct multiboot)
# and Bochs (GRUB on a partitioned HDD) and emit a runtime-computed byte that
# matches a HOST-DERIVED golden. This workflow is the authoritative run of the
# kernel-codegen link harnesses, with KERNEL_CODEGEN_REQUIRE_EMU=1 so a missing
# emulator is a hard failure, never a silent skip. `make test` keeps its
# emulator-free C-golden scope; this is the separate, emulator-graded kernel gate.
#
# Links gated here:
#   L1 = lingo  (link17): "boot and emit a byte" (literal * literal).
#   L2 = toakie (link18): first data-dependent branch on bare metal (control
#        flow + locals), plus a mutation proof that the dual-substrate gate bites.
#   L3 = zonday (link19): installs its own GDT+IDT and catches the CPU's own #DE
#        divide-error fault, vectoring to a compiled-Herbert handler, plus a
#        mutation proof that the fault-catch gate bites.
#   L4 = chosen (link20): turns on paging and proves a mapping by catching the
#        CPU's own #PF (page-fault, vector 14) on the deliberately-unmapped
#        neighbor, vectoring to a compiled-Herbert handler, plus a mutation
#        proof that the paging / #PF-catch gate bites.
#   L5 = link21 (fifth kernel-arc link): writes a page-table entry at a
#        RUNTIME-COMPUTED address (candidate (d), the first computed store),
#        mapping a build-time-unmapped page, then accesses it and the MMU
#        honors it; a broken store #PFs into a fail-closed IDT -> triple-fault.
#        Plus a mutation proof that the runtime-store / MMU-judge gate bites.
#   L6 = link22 (sixth kernel-arc link): DEMAND PAGING / iret-RESUMPTION --
#        touches an unmapped page, takes the CPU's own #PF, a handler maps the
#        page at runtime + flushes the TLB + drops the error code + iret, and the
#        faulting load RE-EXECUTES and reads a seeded frame (the first link whose
#        fault handler RESUMES). Plus a mutation proof that the resume gate bites.

on:
  push:
    branches:
      - main
      - l1-kernel-boot-emit
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

jobs:
  l1-dual-substrate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: install emulators + boot tooling (TEST dependencies)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            qemu-system-x86 \
            bochs bochs-x bochsbios vgabios \
            xvfb \
            grub-pc-bin grub-common \
            parted dosfstools
      - name: assert pinned emulator versions (far-axis -- a silent runner bump must go RED, not silently reshape the spec)
        run: |
          set -e
          q=$(dpkg-query -W -f='${Version}' qemu-system-x86)
          b=$(dpkg-query -W -f='${Version}' bochs)
          echo "qemu-system-x86=$q bochs=$b"
          qemu-system-x86_64 --version | head -1
          bochs --help 2>&1 | head -1 || true
          grub-install --version | head -1
          # 2026-06-19: pin bumped 1:8.2.2+ds-0ubuntu1.16 -> ...1.17 (a Debian PACKAGING revision of the
          # SAME upstream QEMU 8.2.2 -- not an upstream behaviour change). Re-validated in situ: the kernel
          # dual-substrate gates+mutations are re-run under .17 by this very push; their green IS the goldens
          # holding under the new package. Deliberate update per this guard's own instruction.
          test "$q" = "1:8.2.2+ds-0ubuntu1.17" || { echo "QEMU version drift: got $q, pinned 1:8.2.2+ds-0ubuntu1.17 -- update the pin deliberately after re-validating the goldens"; exit 1; }
          test "$b" = "2.7+dfsg-4build5" || { echo "Bochs version drift: got $b, pinned 2.7+dfsg-4build5 -- update the pin deliberately after re-validating the goldens"; exit 1; }
      - name: L1 dual-substrate gate (lingo -- boot and emit a byte)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link17.sh
      - name: L2 dual-substrate gate (toakie -- data-dependent branch + locals)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L2_BOCHS_PROBES="merge_then merge_else return_then return_else" bash bootstrap/tests/run_native_codegen_link18.sh
      - name: L2 mutation proof (the dual-substrate gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link18_mutation.sh
      - name: L3 dual-substrate gate (zonday -- install GDT+IDT, catch the CPU's own #DE)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L3_BOCHS_PROBES="idt_then idt_else idt_lit" bash bootstrap/tests/run_native_codegen_link19.sh
      - name: L3 mutation proof (the fault-catch gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link19_mutation.sh
      - name: L4 dual-substrate gate (chosen -- turn on paging, catch the CPU's own #PF on the unmapped neighbor)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L4_BOCHS_PROBES="page_then page_else page_lit" bash bootstrap/tests/run_native_codegen_link20.sh
      - name: L4 mutation proof (the paging / #PF-catch gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link20_mutation.sh
      - name: L5 dual-substrate gate (write a PTE at a runtime-computed address; the MMU honors it)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L5_BOCHS_PROBES="store_then store_else store_lit" bash bootstrap/tests/run_native_codegen_link21.sh
      - name: L5 mutation proof (the runtime-store / MMU-judge gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link21_mutation.sh
      - name: L6 dual-substrate gate (demand paging -- map a faulting page at runtime and iret-resume)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L6_BOCHS_PROBES="demand_then demand_else demand_lit" bash bootstrap/tests/run_native_codegen_link22.sh
      - name: L6 mutation proof (the iret-resume / demand-paging gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link22_mutation.sh
      - name: L7 dual-substrate gate (first ASYNCHRONOUS interrupt -- remap PIC, program PIT, take the timer IRQ0 to a compiled-body handler)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L7_BOCHS_PROBES="timer_then timer_else timer_lit" bash bootstrap/tests/run_native_codegen_link23.sh
      - name: L7 mutation proof (the asynchronous-timer-IRQ gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link23_mutation.sh
      - name: L8 dual-substrate gate (talkert -- periodic timer, survive+repeat the async IRQ0 via EOI + iret-resume + count-to-N)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L8_BOCHS_PROBES="tick_then tick_else tick_lit" bash bootstrap/tests/run_native_codegen_link24.sh
      - name: L8 mutation proof (the periodic-timer / async-survival gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link24_mutation.sh
      - name: L9 dual-substrate gate (hoopteeter -- cross into 64-bit long mode and emit a 64-bit-only high-dword proof byte)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L9_BOCHS_PROBES="long_then long_else long_lit" bash bootstrap/tests/run_native_codegen_link25.sh
      - name: L9 mutation proof (the long-mode-entry gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link25_mutation.sh
      - name: L10 dual-substrate gate (toggler -- run a genuinely 64-bit COMPILED body, proof byte = the high dword of the body's own 64-bit result)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L10_BOCHS_PROBES="mul_add mul_big" bash bootstrap/tests/run_native_codegen_link26.sh
      - name: L10 mutation proof (the 64-bit-compiled-body gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link26_mutation.sh
      - name: L11 dual-substrate gate (rizzing -- a mainline task spins on a shared memory word the timer ISR bumps and emits only after surviving N async ticks)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L11_BOCHS_PROBES="rizz_then rizz_else" bash bootstrap/tests/run_native_codegen_link27.sh
      - name: L11 mutation proof (the shared-memory-tick synchronization gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link27_mutation.sh
      - name: L12 dual-substrate gate (bluefield -- preemptive context switch; the timer scheduler saves task A's full context, runs task B which clobbers it, restores A so its carried value survives)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L12_BOCHS_PROBES="bf_then bf_else" bash bootstrap/tests/run_native_codegen_link28.sh
      - name: L12 mutation proof (the preemptive-context-switch gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link28_mutation.sh
      - name: L13 dual-substrate gate (trikea / f2 -- widen the 64-bit subset to if/else + let rbp-frame locals; proof byte = high dword of the arm a genuine 64-bit cmp selects)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L13_BOCHS_PROBES="f2_else f2_then" bash bootstrap/tests/run_native_codegen_link29.sh
      - name: L13 mutation proof (the widened-64-bit-subset gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link29_mutation.sh
      - name: L14 dual-substrate gate (trukfit / f3 -- device input; a freestanding image reads one late-bound byte from COM1 via in al,dx and emits f(byte), same image fed X vs Y emits f(X) vs f(Y))
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L14_BOCHS_PROBES="f3_echo f3_inc" bash bootstrap/tests/run_native_codegen_link30.sh
      - name: L14 mutation proof (the device-input value-flow gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link30_mutation.sh
      - name: L15 dual-substrate gate (contigo / f1 -- first data-dependent scheduling decision; a freestanding image reads a late-bound COM1 byte into state and the timer-IRQ0 scheduler branches on it to dispatch one of two seeded tasks)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L15_BOCHS_PROBES="co_lit co_then" bash bootstrap/tests/run_native_codegen_link31.sh
      - name: L15 mutation proof (the data-dependent-scheduling value-flow gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link31_mutation.sh
      - name: L16 dual-substrate gate (cloggard -- first data-dependent MULTI-QUANTUM scheduler; a freestanding timer-IRQ0 run-queue dispatches K=4 WARM tasks over multiple quanta, recomputing next=(inq>>pos)&1 each quantum, so the same image fed X=0x0A emits schedule A,B,A,B and Y=0x05 emits B,A,B,A -- a different ordered trace)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L16_BOCHS_PROBES="cg_lit cg_then" bash bootstrap/tests/run_native_codegen_link32.sh
      - name: L16 mutation proof (the multi-quantum schedule + warmth-provenance gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link32_mutation.sh
      - name: L17 dual-substrate gate (lodger -- the PROGRAM-LIFECYCLE GERM; the kernel runs ONE build-unknown Multiboot module, discovers it via mbinfo, bump-allocates a stack page from the parsed memory map, indirect-calls its runtime entry, collects the answer; same kernel fed module X vs Y emits f(X) != f(Y), module at a DIFFERENT physical address on QEMU vs Bochs)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link33.sh
      - name: L17 mutation proof (the program-lifecycle + bump-allocator + module-witness gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link33_mutation.sh
      - name: L18 dual-substrate gate (trikonderoga -- the RING-3 PRIVILEGE BOUNDARY on the build-unknown module; the kernel runs the discovered module at CPL 3 on the bump-allocated page, a benign module exits via the DPL-3 int 0x30 syscall gate and the compiled body emits f(status), a hostile out 0xE9 faults a general-protection fault at CPL3 and the kernel dumps the CPU-authored ring-cross frame; same kernel fed X vs Y emits f(X) != f(Y))
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link34.sh
      - name: L18 mutation proof (ring-entry + syscall-exit-gate + privileged-op-isolation + resume-order + allocator-recompute gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link34_mutation.sh
      - name: L19 dual-substrate gate (nokta -- MEMORY ISOLATION via paging U S; the kernel runs the build-unknown module at CPL 3 UNDER PAGING with kernel pages Supervisor and the module pages User, so a hostile CPL3 write into kernel RAM faults a page fault errcode 7 CR2 the exact target CS RPL 3 and the write never lands, a hostile read faults errcode 5 confidentiality, a hostile page-table write faults tamper closure, a hostile out still general-protection-faults; benign wrote its own User page and exited via int 0x30, f(X) != f(Y))
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link35.sh
      - name: L19 mutation proof (the U S partition write/read/fetch/PDE/PT-page plus ring boundary under paging plus allocator gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link35_mutation.sh
      - name: L20 dual-substrate gate (sitopia -- the SYSCALL ROUND TRIP; the sandboxed module issues int 0x30 SYS_READ, the kernel at CPL0 polls and reads a late-bound COM1 byte the module cannot read itself (a module in al,dx general-protection-faults), irets back to CPL3 with the byte in eax (the first kernel to module re-entry), the module transforms it in its own code and SYS_EXITs, and the pure-conduit kernel body emits f(byte); same image fed X vs Y emits T(X) != T(Y))
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link36.sh
      - name: L20 mutation proof (the round-trip re-entry plus the two-byte differential vs a dead module plus the disasm body-scan plus the carried ring and paging isolation gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link36_mutation.sh
      - name: L21 dual-substrate gate (geeking -- THE KERNEL OUTLIVES ITS MODULE; with IF=1 at CPL3 a one-shot PIT plus a stale-IRR drain plus an RPL-keyed timer handler async-KILLS a runaway EB FE module via the first CPU-authored async ring-cross frame and CONTINUES to emit f(kill), and fault-to-continue names any CPL3 fault -- general-protection, page-fault, or the generalized debug, divide, invalid-opcode -- so the kernel survives instead of shutdown; a genuine no-errcode kernel fault still panics)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link37.sh
      - name: L21 mutation proof (the async-kill watchdog with stale-IRR drain plus the RPL-keyed handler plus the seeded IF plus the generalized fault-to-continue plus the carried round-trip ring and paging isolation gate must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link37_mutation.sh
      - name: L22 dual-substrate gate (coalgate -- THE KERNEL RUNS ITS FIRST HERBERT-COMPILED MODULE; a new emit mode compiles a Herbert source into a raw position-independent i386 module the frozen geeking kernel runs at ring 3, issuing int 0x30 SYS_READ for a late-bound byte and SYS_EXIT for its compiled transform; the same frozen kernel fed two different compiled modules emits different output, one module fed X vs Y emits T(X) != T(Y), and each emitted module is byte-identical to the STEP-0 target proven on QEMU plus Bochs plus KVM)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link38.sh
      - name: L22 mutation proof (the white-box byte-pin plus the answer-equals-transform check plus the two-byte X to Y differential plus the sys_read-exactly-once and module_byte-banned verifier rules must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link38_mutation.sh
      - name: L23 dual-substrate gate (ouroboros -- THE KERNEL RUNS A COMPILED RECURSIVE ALGORITHM; a new emit mode compiles a recursive multi-function Herbert program into a position-independent i386 module the frozen geeking kernel runs at ring 3, the module reads one byte via int 0x30 SYS_READ and returns its compiled recursive transform via SYS_EXIT through a backward recursive call rel32, the same frozen kernel fed different compiled algorithms tri and dbl and fact and chain emits different output, and each module is byte-identical to the STEP-0 target proven on QEMU plus Bochs plus KVM)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 L23_BOCHS_PROBES="tri fact" bash bootstrap/tests/run_native_codegen_link39.sh
      - name: L23 mutation proof (the white-box byte-pin plus the recursion-is-load-bearing checks echo-instead-of-recurse and base-case-flip and corrupted-backward-call-rel32 and const-bake plus the X to Y differential must bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link39_mutation.sh
      - name: L24 dual-substrate gate (holler -- C3 SYS_WRITE; THE COMPILED RING-3 MODULE EMITS ITS OWN MULTI-BYTE OUTPUT; a new kernel adds a do_write int 0x30 eax=2 arm with a software access_ok bounds-check of a module-supplied buffer against its User page read by value, the first confused-deputy kernel-contract surface, and a new module emit mode compiles a Herbert source that sys_writes le32(b) and le32(3b), the emitted kernel is byte-identical to holler_ref.build_elf and the emitted module byte-identical to holler_ref.target_module and relays its own bytes by value with X vs Y differing on QEMU plus Bochs, a hostile kernel-pointer or page-straddling SYS_WRITE is rejected, and the holler kernel still async-kills a runaway and names plus continues a CPL3 fault)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link40.sh
      - name: L24 mutation proof (the master prefix byte-pin catches every do_write mutation structurally and M-nobounds leaks kernel code while M-noptrlo and M-noendhi leak via the dropped ptr-low and end-high branches and M-fakewrite and M-norelay and M-bakebounds fail gradewrite, each bite proven on silicon)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link40_mutation.sh
      - name: L25 dual-substrate gate (mmj -- THE UNION; the first compiled ring-3 module that BOTH computes with recursion and emits its OWN multi-byte output, a new emit mode module-mmj on the FROZEN holler kernel where down(N) recursively sys_writes N words so the output LENGTH equals f(runtime input), unfakeable by ouroboros which has no op 48 or holler which is straight-line with a compile-time-fixed count; the emitted module is byte-identical to mmj_ref.target_module(down) and carries a BACKWARD E8 self-call via assert_backward_call, and on QEMU plus Bochs it emits EXACTLY N write-frames le32(N..1) for fed N with X vs Y output LENGTH differing, while the multiboot32-holler kernel stays byte-identical to holler_ref.build_elf -- module-only/additive)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link41.sh
      - name: L25 mutation proof (the byte-pin plus assert_backward_call catch the non-recursive jmp-loop forge that grades GREEN at runtime since recursion is invisible in a syscall trace, while M-fixedcount goes RED on the variable-LENGTH count pin and M-content goes RED on the content pin)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link41_mutation.sh
      - name: L26 dual-substrate gate (chiefturbo -- RUNTIME-INDEXED MEMORY; the compiled ring-3 module's first register-indexed SS-relative load/store into a runtime-filled buffer on its User page, a new emit mode module-chiefturbo on the FROZEN holler kernel where the module fills a buffer with N equals 40 random 24-bit input words via indexed STORE then answers N runtime-index queries reading buf at k via indexed LOAD, so reproducing the gather output genuinely requires register-indexed memory since 40 words exceed 31 named slots and 24-bit words cannot pack two-per-slot and the data is random and held-back; the emitted module is byte-identical to chiefturbo_ref.target_module and carries an SS-relative SIB indexed op via assert_indexed_load, and on QEMU plus Bochs it gathers data at idx j with gx versus gy index walks producing different output, while the multiboot32-holler kernel stays byte-identical to holler_ref.build_elf -- module-only/additive TYPE-I)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link42.sh
      - name: L26 mutation proof (the byte-pin plus assert_indexed_load catch the non-indexed baked forge that grades GREEN at runtime since a runtime trace cannot witness indexed memory for KNOWN data, while M-wrongidx goes RED on the random-access content pin and M-constidx goes RED on the read-count and content pins)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link42_mutation.sh
      - name: L27 dual-substrate gate (mumbani -- MULTI-PAGE WORKING MEMORY; the kernel hands the compiled ring-3 module a working set LARGER THAN ONE PAGE, D20 second installment, via a new emit mode multiboot32-mumbani that widens the bump allocator to K equals 4 contiguous frames plus a 4-PTE User-flip and a new module mode module-mumbani equals module-mmj with sys_read relaxed to at-least-once; the forcing program is a recursive REVERSE of N equals 400 held-back random 24-bit words read down the recursion and emitted on unwind, so since Herbert has no loops and no TCO the 400-deep stack EXCEEDS one page, and the SAME module RUNS on the 4-page kernel emitting the held-back stream reversed with write esps spanning more than one page while it #PFs mid-descent emitting ZERO words on the FROZEN 1-page holler kernel; the emitted module is byte-identical to mumbani_ref.target_module and the kernel byte-identical to mumbani_ref.build_elf npages 4 plus a white-box assert of the 0x4000 alloc-size and 4 User-flip blocks, and the multiboot32-holler kernel plus module-mmj plus module-chiefturbo stay byte-identical -- additive TYPE-II)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link43.sh
      - name: L27 mutation proof (M-onepage goes RED since a 1-page alloc #PFs the reverse descent proving the multi-page ALLOCATION is load-bearing, M-flip1 goes RED since flipping only the top PTE faults when the descent crosses into an unmapped lower page proving the 4-PTE User-map is load-bearing, and M-forward goes RED on the reversal content pin plus is byte-different from target_module proving the reversal witness and the emitter byte-pin bite)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link43_mutation.sh
      - name: L28 dual-substrate gate (tandem -- THE KERNEL RUNS TWO DISTINCT LOADED PROGRAMS at once, peer-isolated and kernel-scheduled, via a new emit mode multiboot32-tandem that parses mods_count equals 2 and bump-allocates TWO regions with a per-program User Supervisor PTE-flip at the yield boundary plus a SYS_YIELD cooperative scheduler and a one-cell A-to-B mailbox, and a new module mode module-tandem equals the module-mmj subset plus the new yield op 52; program A reads N held-back random words echoes each and yields each while program B receives each via yield and writes 3 times w cross-yield-derived, the debugcon write-frames alternate A B A B as the kernel-scheduled witness, gx and gy differ and cross-grade is RED, and a hostile A writing into B's region #PFs with CR2 in B's window proving PEER isolation, also re-run byte-identical on KVM real silicon; the emitted programs are byte-identical to tandem_ref.module_A_real and module_B_real and the kernel byte-identical to tandem_ref.build_elf, and module-mmj module-mumbani module-chiefturbo plus multiboot32-mumbani holler stay byte-identical -- additive TYPE-II)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link44.sh
      - name: L28 mutation proof (M-singleslot goes RED since reverting mods_count to equals 1 makes the kernel reject two modules and emit nothing, M-noflip goes RED since flipping both programs User at boot removes peer isolation so the hostile-peer write no longer #PFs, and M-noswap goes RED since a yield that does not switch lets A run to exit before B runs so B emits 0 words and the interleave is A-only not A B A B)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link44_mutation.sh
      - name: L29 dual-substrate gate (tickover -- FAIR TIMER-PREEMPTION between two programs, a program that NEVER yields cannot starve its peer, via a new emit mode multiboot32-tickover additive on the frozen tandem lineage, a periodic PIT plus IRQ0 unmasked plus IF=1 module frames so the timer preempts a CPL3 module, and a vec-0x20 PREEMPTIVE handler that saves and restores the FULL preempted context the GP set plus eflags plus eip plus useresp a WIDENED TCB versus tandem cooperative eip esp ebp, plus the per-program User Supervisor PTE-flip; KERNEL-emit only with hand-assembled fixture probes since a stack-machine compiled module holds no live register to lose, program A a register-carry SPINNER that never yields holds edx and DF across a pure spin and emits le32 VA only if both survived every preempt the full-context proof while program B a worker reads N held-back random words and emits each proving it RAN despite A never yielding no starvation, gx and gy differ and cross-grade is RED, and a hostile A writing into B region #PFs err 7 and reading #PFs err 5 proving PEER isolation under preemption, all re-run byte-identical on KVM real silicon and Bochs; the kernel is byte-identical to tickover_ref.build_elf and tandem plus module-tandem mumbani mmj stay byte-identical -- additive TYPE-II)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link45.sh
      - name: L29 mutation proof (M-coop goes RED since IF=0 module frames let the non-yielding spinner A run forever so the worker B starves, M-noswitch goes RED since a handler that EOIs without switching lets A keep running so B starves, M-minimal goes RED since saving only the cooperative eip esp ebp TCB loses A edx across the preempt so A emits the wrong value, M-noeflags goes RED since saving the GP set but not eflags loses A DF so A emits the sentinel, and M-noflip goes RED since flipping both programs User at boot removes peer isolation so the hostile-peer write no longer #PFs)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link45_mutation.sh
      - name: L30 dual-substrate gate (rollcall -- RUNTIME-K PROCESS TABLE plus RUN-QUEUE, the scheduler becomes a DATA STRUCTURE; a new emit mode multiboot32-rollcall additive on the frozen tickover lineage generalizes tickover hardcoded-TWO two TCBs UNROLLED cur xor 1 to a TCB ARRAY indexed by a round-robin run-queue loop with K read from mods_count at RUNTIME 1 to MAXPROC 8; the SAME kernel binary runs K equals 3 and 5 and 7 module images all programs make progress -- proc0 a non-yielding SPINNER emits le32 VA only if its full GP plus eflags survived the indexed-TCB save and restore across the preempt, and procs 1 to K-1 WORKERS each emit a held-back baked token proving they RAN since preemption got the CPU off the spinner a cooperative kernel hangs; graded on per-program COMPLETENESS not interleave order, no phantom program, switch counter at least K; a hostile proc0 writing a peer region #PFs err 7 and reading #PFs err 5 proving peer isolation under the runtime-K flip; byte-identical on QEMU-TCG and KVM real silicon and Bochs; the kernel is byte-identical to rollcall_ref.build_elf and tickover tandem mumbani stay byte-identical -- additive TYPE-II)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link46.sh
      - name: L30 mutation proof (M-cap2 goes RED since clamping K to 2 tickover-style starves proc 2 and beyond, M-norobin goes RED since toggling 0 to 1 instead of round-robin never schedules proc 2 and beyond, M-nowake goes RED since never waking the spinner on the last worker exit hangs forever, M-nolive goes RED since never decrementing the live count means the spinner is never woken and the kernel never finalizes so it hangs, M-coop goes RED since IF=0 module frames let the non-yielding spinner run forever so the workers starve, M-noswitch goes RED since a handler that EOIs without switching starves the workers, M-noflip and M-noflip0 go RED since the per-process User Supervisor flip is missing so a proc faults or isolation breaks, M-minimal goes RED since the cooperative eip esp ebp TCB loses the spinner edx across the preempt, and M-noeflags goes RED since dropping eflags loses the spinner DF)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link46_mutation.sh
      - name: L31 dual-substrate gate (tenement -- bare-metal MEMORY RECLAMATION, a ring-3 program exits and its 1-page region is FREED and REUSED by a waiting program; a new emit mode multiboot32-tenement additive on the frozen rollcall lineage mints only MSLOTS==2 region pages but parses N procs from mods_count, so procs M to N-1 start WAITING with alloc_lo==0, the round-robin pick skips a region-less waiting proc, and SYS_EXIT scans for the first waiting proc and HANDS it the exiting proc region by copying alloc_lo and alloc_hi cur to w which is the same physical page so the page is REUSED; the forcing program is N==6 all-worker modules sharing M==2 region pages, each worker emits its own held-back token, graded on per-worker COMPLETENESS plus the at-most-M distinct region pages reuse pin plus each minted page hosting at least 2 distinct workers reuse-over-time, write esp inside a minted page, switch counter; THE DIFFERENTIAL the FROZEN rollcall kernel fed the SAME 6 workers mints 6 distinct pages and grades RED since it cannot reclaim, proving the reuse observable is genuinely new; byte-identical on QEMU-TCG and KVM real silicon and Bochs; the kernel is byte-identical to tenement_ref.build_elf and rollcall tickover stay byte-identical -- additive TYPE-II, pays D16 reclamation half and D20 free-grow core)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link47.sh
      - name: L31 mutation proof (M-noreclaim goes RED since removing the SYS_EXIT handoff leaves the waiting procs with no region so the scheduler skips them forever and they never emit their tokens, M-noremap goes RED since handing over alloc_lo but NOT alloc_hi lets the waiter be scheduled yet fresh-start with useresp 0 so its first push #PFs and its token is missing, and M-noskip goes RED since inverting the waiting-skip selects a region-less waiting proc and iret into a zero region #PFs; each mutation is ALSO caught structurally by assert_tenement which pins the contiguous reachable lo plus hi handoff motif and the waiting-skip)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link47_mutation.sh
      - name: L32 dual-substrate gate (homestead -- DEMAND-PAGED STACK GROWTH, the kernel grows a ring-3 program stack ON DEMAND; a new emit mode multiboot32-homestead additive on the frozen tenement lineage gives each proc a 1-page committed region PLUS a GROWMAX-page P equals 0 not-present grow window below it, and on a NOT-PRESENT page-fault err.P equals 0 in that window the kernel COMMITS the faulting page on demand setting its PTE to present plus RW plus User identity frame and IRET-resumes the faulting push -- genuine demand paging not a static map since the page is genuinely not-present until the demand fault commits it unlike tenement and mumbani which only ever toggle the User bit on always-present pages; the forcing program is a single recursive GROWER Herbert has no loops or TCO so N equals 400 items needs about N stack frames more than the 1-page region, it reads a held-back seed byte via SYS_READ late-bound over COM1 generates N distinct words from the seed and emits them REVERSED on the unwind, and the FULL held-back reversed stream comes out only because the stack grew, demand commits are WITNESSED as C2 to C3 frames err.P equals 0 PTE P 0 to 1 cr2 in the grow window the temporal proof; THE DIFFERENTIAL the FROZEN tenement kernel fed the SAME grower grades RED since a fixed 1-page region over an all-present map plus a terminal page-fault handler kills the descent with a protection fault, proving demand-growth is genuinely new; byte-identical on QEMU-TCG and KVM real silicon and Bochs; the kernel is byte-identical to homestead_ref.build_elf and tenement rollcall tickover mumbani stay byte-identical -- additive TYPE-II, pays D20 grow half)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link48.sh
      - name: L32 mutation proof (M-nogrow goes RED since removing the demand-commit page-fault branch terminal-kills the grower on its first over-page push so 0 output and 0 commit witnesses, M-noclear goes RED since skipping the boot P-clear leaves the grow window present and Supervisor so a CPL3 push takes a PROTECTION fault err.P equals 1 which the err.P equals 0 demand branch does not handle so the grower is killed, and M-eager goes RED since eager-mapping the whole grow window present plus User up front lets the grower emit the FULL correct output but takes ZERO not-present faults so ZERO demand commits -- the KEY mutation distinguishing demand-commit from a fixed-large or eager reserve same output missing the temporal commit witness; each mutation is ALSO caught structurally by assert_homestead which pins the boot P-clear the err.P equals 0 fault gate and the demand-commit value and store)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link48_mutation.sh
      - name: L33 dual-substrate gate (furlough -- BLOCKING SYS_READ block and wake, a ring-3 program that issues SYS_READ on input not yet available is DESCHEDULED parked with zero quanta by the kernel instead of busy-polling COM1 with IF equals 0 and FREEZING the machine, peers keep running, and when the byte arrives the kernel WAKES the parked reader delivers the byte into its saved eax and it resumes after int 0x30; a new emit mode multiboot32-furlough additive on the frozen homestead lineage adds a blocked array TCB state plus a scheduler pick that SKIPS blocked procs plus a BLOCK arm in do_read snapshot the int 0x30 frame mark blocked switch away plus a WAKE arm in sched_switch re-poll COM1 deliver into the wakee t_eax unblock plus an idle path all-parked re-poll until a byte wakes one plus a per-proc disp dispatch counter; the CONFIRMED DEFECT removed is that do_read polls COM1 inside a DPL-3 INTERRUPT gate IF equals 0 at CPL0 and the timer skips CPL0 ticks so a kernel busy-poll is un-preemptible by design and sti or trap-gate cannot fix it, the only fix-class for a naive non-yielding reader is kernel-side deschedule; the forcing programs are a naive reader A one mov eax 0 int 0x30 then emit le32 RDR_TAG or byte and two token-emitter peers B C, graded on TWO runs RUN-2 byte delivered the reader is parked peers run then the reader is woken with the correct byte emits its token a wake witness w 0 byte matches disp tiny clean finalize, and RUN-1 byte withheld forever the reader is parked but peers still run their tokens appear the freeze is fixed, plus a seed-differential a different delivered byte gives the new byte token data-dependence; THE DIFFERENTIAL the FROZEN homestead kernel fed the SAME 3 programs with A byte withheld grades RED since A busy-polls IF equals 0 and FREEZES the machine so B C never run, proving block and wake is genuinely new; byte-identical on QEMU-TCG and KVM real silicon and Bochs; the kernel is byte-identical to furlough_ref.build_elf and tenement rollcall tickover homestead stay byte-identical -- additive TYPE-II, removes the IF equals 0 SYS_READ freeze latent defect)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link49.sh
      - name: L33 mutation proof (M-noblock goes RED since reverting do_read to the IF equals 0 busy-poll freezes the machine on the naive reader so peers absent, M-noblockflag goes RED since the block arm snapshots the frame but does not set blocked so the reader stays runnable and resumes after int 0x30 with a stale t_eax wrong byte, M-restart goes RED since descheduling without a blocked state and rewinding eip to re-execute int 0x30 the runnable-retry forge FIXES the freeze RUN-1 GREEN but re-dispatches the reader every cycle with no wake witness so RUN-2 RED -- the KEY mutation the freeze-fix alone is under-determined only correct wake and parked-not-spinning is block and wake, M-noskipblk goes RED since inverting the pick blocked-skip picks a PARKED proc and wedges, M-nowake goes RED since the wake arm reads and witnesses but never clears blocked so the reader never resumes, M-nodeliver goes RED since the wake arm clears blocked but never stores the byte into t_eax so the reader resumes with garbage; each mutation is ALSO caught structurally by assert_furlough which pins the block-arm frame snapshot plus blocked set the blocked-skip the wake-arm deliver plus clear and the disp counter)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link49_mutation.sh
      - name: L34 dual-substrate gate (tessera -- SHARED MEMORY via a NON-IDENTITY ALIASED FRAME, the first time vaddr is not equal to paddr in the stack; every prior link mapped virtual page g to physical frame g identity PTE g equals g times 4096 plus 3 so memory capabilities were permission or identity tricks an identity-backed reserve can forge, tessera installs AT RUNTIME ONE shared physical frame F mapped at TWO distinct virtual pages Va not equal Vb with F not equal Va and F not equal Vb the first PTEs where the physical frame number is not the virtual page number; a producer ring-3 program writes a late-bound multi-word payload THROUGH Va and a consumer reads it back THROUGH Vb a plain CPL3 mov with no kernel copy and emits each word, genuine zero-copy shared memory for a payload larger than tandem 4-byte register mailbox can carry, the consumer could only have read the words through the shared frame since its own region is peer-isolated the mailbox cannot carry a multi-word payload and the seed is late-bound over COM1 so it cannot be baked; THE DIFFERENTIAL the FROZEN furlough kernel fed the SAME pair leaves Va Vb identity and Supervisor so the producer page-faults writing the shared window and the consumer page-faults reading it so the consumer never sees the payload RED, proving the non-identity alias is genuinely new; byte-identical on QEMU-TCG and KVM real silicon and Bochs; the kernel is byte-identical to tessera_ref.build_elf and furlough homestead tenement rollcall tickover stay byte-identical additive TYPE-II; minimal atom one pre-installed aliased frame no runtime free-frame allocator no per-process CR3 no ELF loader, per-process address spaces compose from this primitive as the motivated successor)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link50.sh
      - name: L34 mutation proof (M-noinstall goes RED since skipping the alias install leaves Va Vb identity and Supervisor so a CPL3 access page-faults, M-identity goes RED since installing identity Va to Va and Vb to Vb still User makes the windows accessible but the frames DISJOINT so the consumer reads its OWN window never the producer payload the KEY mutation the homestead M-eager analogue proving the NON-IDENTITY ALIASING not the User permission is the load-bearing observable, M-onlyone goes RED since aliasing only Va leaves Vb identity-disjoint so the consumer never sees the payload, and M-supervisor goes RED since aliasing both to F but without the User bit page-faults a CPL3 access to the shared window; each mutation is ALSO caught structurally by assert_tessera which pins PTE Va to F and PTE Vb to F with F not equal Va and F not equal Vb aliased and non-identity)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link50_mutation.sh
      - name: L35 dual-substrate gate (cleave -- COPY-ON-WRITE the first time the kernel COPIES a page on demand; one ring-3 program maps ONE shared physical frame F at a WRITABLE alias VW with present read-write user bits and a READ-ONLY alias VR with present user bits and no write bit both non-identity aliases of the SAME frame, it fills F through VW then stores a marker THROUGH the read-only VR which traps a write-protection page-fault err present 1 write 1 user 1, the kernel COW arm allocates a FRESH frame F prime from a reserved pool COPIES F to F prime with rep movsd remaps PTE VR to F prime with present read-write user bits private and writable and IRET-resumes the store into the copy, then the program reads back VW words which equal the payload since the shared frame F is UNCHANGED and VR words which equal the payload except the one written word which equals the marker the private copy; output-forced WITHIN one execution unlike per-process CR3 which is cross-process and unobservable on one CPU since a single global page directory rewritten on each switch reproduces it; THE DIFFERENTIAL the FROZEN tessera kernel fed the SAME prober maps VR WRITABLE so the store does not fault and lands in the shared frame F so VW reads the marker RED proving copy-on-write is genuinely new; byte-identical on QEMU-TCG and KVM real silicon and Bochs; the kernel is byte-identical to cleave_ref.build_elf and tessera furlough homestead tenement rollcall tickover stay byte-identical additive TYPE-II; minimal atom one shared frame plus a fixed-vaddr read-only and writable alias pair plus a reserved bump pool for copies no runtime free-frame allocator no general arena no per-process address spaces no ELF loader)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link51.sh
      - name: L35 mutation proof (M-noinstall goes RED since skipping the alias install leaves VW VR identity and Supervisor so the CPL3 store page-faults terminally, M-vrwritable goes RED since installing VR writable means the store does not fault and lands in the shared frame F so VW reads the marker, M-videntr goes RED since installing VR to a disjoint identity frame makes the prober read an empty window never the payload, M-cowshare goes RED THE KEY since the COW arm flips VR writable over the SHARED frame F with no private copy so the store corrupts F and VW reads the marker proving the PRIVATE COPY not the permission flip is the load-bearing observable and that the divergence is forced on output WITHIN one execution, M-nocopy goes RED since allocating F prime but skipping the rep movsd copy leaves the VR copy holding garbage not the preserved payload, M-shortcopy goes RED since copying only the first words and not the full 4 KiB page leaves the DEEP word at offset 0xFFC which the prober fills and reads back uncopied so it output-forces the FULL page copy, and M-noremap goes RED since copying F to F prime but not remapping VR makes the resumed store re-fault on the still read-only VR; each mutation is ALSO caught structurally by assert_cleave which pins PTE VW to F present read-write user and PTE VR to F present user read-only with F not equal VW and F not equal VR a full-page rep movsd copy of 1024 dwords the copy destination loaded from the cow_next pool and the exact remap of VR to the private copy F prime; the COW arm also gates on err.U so a supervisor write never wrongly copies-on-write and bounds-checks the cow_next pool)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link51_mutation.sh
      - name: L36 dual-substrate gate (lethe -- ALIAS-REMAP plus TARGETED TLB INVALIDATION the first time the kernel invalidates a stale TLB entry; one ring-3 prober installs THREE non-identity aliases A and V both mapping the shared frame F and B mapping a second frame F prime, it WARMS the mapping by writing x through V which populates F and reading A which equals x and caches the V to F translation in the TLB, then a SYS_REMAP int 0x30 sets PTE V to F prime and the link executes invlpg of V evicting EXACTLY the one stale entry not a heavy cr3 flush, then the prober writes y through V which does a fresh walk to F prime and reads back A equals x proving F is UNCHANGED no ghost corruption and V equals y and B equals y both reading F prime; output-forced WITHIN one execution since the corruption when invalidation is dropped is observed in A a DIFFERENT alias than the one written; THE DIFFERENTIAL the FROZEN tessera kernel fed the SAME prober has no SYS_REMAP arm so V stays mapped to F and the post-remap write y lands in F and corrupts the witness A RED proving the alias-remap plus invalidation is genuinely new; byte-identical on QEMU-TCG and KVM real silicon and Bochs; the kernel is byte-identical to lethe_ref.build_elf and cleave tessera furlough homestead tenement rollcall tickover stay byte-identical additive TYPE-II; the stale-TLB witness is empirical on the tested substrates since x86 permits but does not require a CPU to retain a stale entry which is why all three substrates including the physical CPU via KVM confirm it)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 bash bootstrap/tests/run_native_codegen_link52.sh
      - name: L36 mutation proof (M-noinvlpg goes RED since dropping the invlpg leaves the stale V to F entry so the post-remap write y lands in the GHOST frame F and corrupts the witness alias A, M-noremap goes RED since omitting the PTE V to F prime write leaves V mapped to F so y lands in F and corrupts A, M-sameframe goes RED since remapping V to F itself means the write still lands in F and corrupts A, M-noinstall goes RED since skipping the three alias installs leaves the windows identity and Supervisor so the CPL3 store page-faults terminally, the no-warm DIFFERENTIAL goes RED since a prober that skips the warm never populates F so even the genuine kernel reads an un-populated witness proving the warm is the load-bearing setup, M-cr3insteadofinvlpg goes GREEN on output since a full cr3 flush is behaviorally correct but assert_lethe REJECTS it because the remap arm contains a mov cr3 and no targeted invlpg of V proving the surgical primitive not the heavy flush is what is pinned, and M-cr3edx goes GREEN on output but assert_lethe REJECTS it too since the cr3 reject was widened from mov cr3 eax only to every mov cr3 register form closing a gap a cross-model review found; each mutation is ALSO caught structurally by assert_lethe which pins the three non-identity alias installs and the remap arm PTE V to F prime followed contiguously by invlpg of V with no mov cr3 of any register in the arm)
        run: KERNEL_CODEGEN_REQUIRE_EMU=1 KERNEL_CODEGEN_MUTATION=1 bash bootstrap/tests/run_native_codegen_link52_mutation.sh
