# Codex Status - 2026-06-24

Branch: `codex/sovereignty-audit-20260624`
Worktree: `/Users/ben/Desktop/1/herbert-sovereignty-audit-20260624`
Verified branch tip before this note: `7e4b95fcfeb98d25f16e5eac4dde99ed5a46078c`
Draft PR: `https://github.com/CommonHerb/herbert/pull/3`

## Pushed Status

Pushed commits before this note:

- `277a01c docs: add sovereignty audit`
- `7e4b95f fix: split portable and linux verification`

This status note is intended to be pushed as the next commit on the same branch.

## What Changed

- Added `docs/codex/sovereignty-audit.md`, a verdict-first sovereignty audit tied
  to source, local commands, CI, and current GitHub state.
- Split the verification topology:
  - `make verify-local` is now a portable source/governance ladder.
  - `make verify-linux` preserves the old full Linux/x86_64 ladder under a
    truthful name.
- Added `tools/check_verify_targets.py` to guard the target split and the
  Linux-host refusal message.
- Updated `README.md`, `VERIFYING.md`, and `ROADMAP.md` so the public truth
  surface no longer claims a present C interpreter or a portable full native
  ladder.
- Opened draft PR #3 as autonomous work, explicitly not for merge without Ben.

## What Passed

Local Darwin/arm64:

- `python3 tools/check_verify_targets.py`
- `make verify-local`
- `make -n verify-linux`
- `bash tools/check_full_test_host.sh` failed intentionally with the Linux-only
  refusal and now names both `make test` and `make verify-linux`.
- `git diff --check`

GitHub Actions for `7e4b95f`:

- Push `check`: success, run `28085736645`.
- PR `check`: success, run `28085860265`.
- PR `kernel-codegen-l1` / `l1-dual-substrate`: success, run `28085860284`,
  completed at `2026-06-24T09:38:19Z`.

## What Failed

- Initial audit baseline: `make verify-local` failed on Darwin/arm64 because it
  included `make test`, and `make test` correctly requires Linux/x86_64.
- Initial representative native gate: `make lexer-native` failed on Darwin/arm64
  because the committed gen-1 seed is an x86_64 Linux ELF and produced no local
  runnable ELF on this host.
- No post-fix local or GitHub check failed at the verified branch tip.

## Current Audit Verdict

Herbert is materially C-interpreter-free in the current checkout. The remaining
trust anchors are the once-C-minted `gen1.seed`, committed goldens, shell/Python
harnesses, Linux/x86_64 execution, emulator tooling, and the C governance scanner
`tools/scan.c`. The verification split fix improves honesty: the local command
now says only what this Mac can prove, while the Linux ladder keeps the stronger
native proof surface.

## Remaining Risks

- The textual-seed/trusting-trust hardening remains deferred.
- `SWITCHOVER.md`, `bootstrap/seed/README.md`, and some historical wording still
  need post-switchover cleanup.
- Kernel links remain proof-mode/frontier surfaces, not a finished general OS or
  arbitrary compiler-correctness proof.
- This status-note commit itself still needs to be pushed and rechecked after it
  exists as a real branch tip.

## Exact Next Autonomous Step

Push this status note, verify the resulting GitHub state, then continue with the
next smallest truth-surface cleanup: mark `SWITCHOVER.md` as historical
post-event material and fix the stale seed README hash/provenance wording.

---

# Codex Follow-up Status - 2026-06-24

Branch: `codex/sovereignty-audit-20260624`
Worktree: `/Users/ben/Desktop/1/herbert-sovereignty-audit-20260624`
Verified branch tip before this follow-up: `d464b5e8070a8e52492f96b960f53a42a2f54f7a`
Draft PR: `https://github.com/CommonHerb/herbert/pull/3`

## Pushed Status

The branch was pushed through `d464b5e8070a8e52492f96b960f53a42a2f54f7a` before
this follow-up. GitHub Actions for that pushed tip:

- Push `check`: success, run `28089776513`.
- PR `check`: success, run `28089778258`.
- PR `kernel-codegen-l1` / `l1-dual-substrate`: success, run `28089778253`,
  completed at `2026-06-24T10:49:55Z` in `1h5m5s`.

This follow-up note is intended to be committed and pushed with the
post-switchover truth-surface cleanup; that new pushed tip will still need its
own remote checks.

## What Changed

- Marked `SWITCHOVER.md` as a historical record of the executed `castoff`
  switchover instead of a pending greenlight plan.
- Updated `bootstrap/seed/README.md` so it says the C interpreter is retired and
  names the current committed seed hash.
- Updated switchover script and manifest comments so retained machinery is
  described as post-castoff audit/regression material.
- Updated `ROADMAP.md` so self-hosting prerequisites do not revive C as a live
  oracle.
- Updated the audit document to mark repaired truth-surface findings as repaired
  on this branch.

## What Passed

Local Darwin/arm64 on the follow-up diff:

- Stale-phrase scan over the edited truth surfaces: no live hits.
- `bash -n bootstrap/tests/run_switchover_dryrun.sh bootstrap/tests/apply_switchover.sh`
- `git diff --check`
- `make verify-local`

## What Failed

- No local check failed for this follow-up diff.
- Remote checks for the follow-up commit are not yet run because the commit does
  not exist until this note is committed and pushed.

## Current Audit Verdict

Herbert's branch truth surfaces now better match the source and history:
`castoff` retired the C interpreter, `tools/scan.c` is a kept governance scanner,
and the committed gen-1 seed remains the major once-C-minted trust boundary. The
verification topology is split honestly between portable local checks and
Linux/x86_64 gates.

## Remaining Risks

- Textual-seed/trusting-trust hardening remains deferred.
- The missing `LEDGER.md` reference in `BOOTSTRAP-RESPONSIBILITIES.md` still
  weakens the claim trail for D13/D16-style far-axis/tethered statements.
- Kernel links remain proof-mode/frontier surfaces, not a finished general OS or
  arbitrary compiler-correctness proof.
- The follow-up commit must be pushed and watched in GitHub Actions.

## Exact Next Autonomous Step

Commit and push this follow-up cleanup, watch GitHub Actions for the new tip,
then repair or replace the missing `LEDGER.md` reference with a live source of
truth.

---

# Codex Follow-up Status - 2026-06-24T12:42Z

Branch: `codex/sovereignty-audit-20260624`
Worktree: `/Users/ben/Desktop/1/herbert-sovereignty-audit-20260624`
Verified branch tip before this follow-up: `04c00207363422e8b9c32bbbc95db56b47acdf92`
Draft PR: `https://github.com/CommonHerb/herbert/pull/3`

## Pushed Status

The branch was pushed through `04c00207363422e8b9c32bbbc95db56b47acdf92`
before this follow-up. GitHub Actions for that pushed tip:

- Push `check`: success, run `28093421097`.
- PR `check`: success, run `28093422813`.
- PR `kernel-codegen-l1` / `l1-dual-substrate`: success, run `28093422814`,
  attempt 2, completed at `2026-06-24T12:39:53Z` in `1h7m45s`.

Attempt 1 of the same kernel run failed transiently at L21
`run_native_codegen_link37.sh`; attempt 2 passed L21 and the full workflow for
the same branch SHA.

## What Changed

- Replaced the missing `LEDGER.md` reference in `BOOTSTRAP-RESPONSIBILITIES.md`
  with live proof surfaces:
  - `bootstrap/tests/run_tests.sh` for the muster classification that retired
    C-GC instrumentation with C.
  - `bootstrap/tests/run_native_codegen_link47.sh` plus its mutation proof for
    far-axis native reclamation.
  - `.github/workflows/kernel-codegen-l1.yml` L31 steps for authoritative CI
    execution of those gates.
- Updated `docs/codex/sovereignty-audit.md` so the missing-ledger finding is now
  recorded as repaired on this branch.

## What Passed For This Commit

Current pushed tip `04c00207363422e8b9c32bbbc95db56b47acdf92` before this
commit:

- Local Darwin/arm64 `make verify-local` passed before this follow-up started.
- PR checks were green: push `check`, PR `check`, and `kernel-codegen-l1`.

Fresh local checks for this follow-up diff:

- `rg -n 'LEDGER\.md'` over live truth surfaces: only historical/repaired audit
  mentions remain.
- `git diff --check`
- `make verify-local`

This follow-up commit still needs push and remote checks after it exists.

## What Failed

- No local check failed during this follow-up.
- Historical note: kernel attempt 1 for run `28093422814` failed at L21, but the
  rerun attempt 2 passed the full workflow for the same SHA.

## Current Audit Verdict

The responsibility map no longer points at a missing file for D16/native
reclamation. Its claim path now leads to executable source and CI: the retired
C-only assertion is documented in the test harness, and the live reclamation
capability is proved by link47, mutation proof, and the kernel workflow.

## Remaining Risks

- Textual-seed/trusting-trust hardening remains deferred.
- Kernel links remain proof-mode/frontier surfaces, not a finished general OS or
  arbitrary compiler-correctness proof.
- Some kernel PASS prose may still overstate KVM or substrate proof when a local
  run skips unavailable emulators; this needs a focused wording/log audit.
- This follow-up commit must be locally verified, pushed, and watched in GitHub
  Actions.

## Exact Next Autonomous Step

Commit and push this follow-up diff, watch the GitHub checks for the new tip,
then audit kernel PASS/log wording for substrate overclaims.

---

# Codex CI Closeout - 2026-06-24T13:56Z

Branch: `codex/sovereignty-audit-20260624`
Worktree: `/Users/ben/Desktop/1/herbert-sovereignty-audit-20260624`
Verified branch tip: `f4db254c904b64547cc73b2207e6e98d39b87257`
Draft PR: `https://github.com/CommonHerb/herbert/pull/3`

## Pushed Status

The branch was pushed to `origin/codex/sovereignty-audit-20260624` at
`f4db254c904b64547cc73b2207e6e98d39b87257`. Fresh `git ls-remote origin
refs/heads/codex/sovereignty-audit-20260624` returned the same SHA.

## What Changed

No additional code or claim-surface change was made after `f4db254`. This
closeout records the completed remote verification for that commit.

## What Passed

- Local `git diff --check` and `make verify-local` passed before `f4db254` was
  committed.
- PR `check`: success, run `28099395549`, job `83197042898`, duration `1m48s`.
- Push `check`: success, run `28099393219`, job `83197035169`, duration `1m50s`.
- PR `kernel-codegen-l1` / `l1-dual-substrate`: success, run `28099395553`,
  job `83197043099`, attempt 1, completed at `2026-06-24T13:52:21Z` in
  `1h7m25s`, head SHA `f4db254c904b64547cc73b2207e6e98d39b87257`.

## What Failed

No local or remote check failed for `f4db254`.

## Current Audit Verdict

The stale `LEDGER.md` responsibility-map reference is repaired and backed by
live sources plus a green authoritative GitHub kernel-codegen run for the exact
pushed SHA. The broader audit verdict is unchanged: Herbert has strong
executable sovereignty proof surfaces, but textual-seed/trusting-trust
hardening and overclaim-resistant kernel wording remain important next work.

## Remaining Risks

- Textual-seed/trusting-trust hardening remains deferred.
- Kernel links remain proof-mode/frontier surfaces, not a finished general OS or
  arbitrary compiler-correctness proof.
- Some kernel PASS prose may still overstate substrate proof when local runs
  skip unavailable emulators; this still needs a focused wording/log audit.

## Exact Next Autonomous Step

Audit kernel PASS/log wording for substrate overclaims, then make the smallest
verified wording or harness change that keeps claims tied to actual runnable
evidence.
