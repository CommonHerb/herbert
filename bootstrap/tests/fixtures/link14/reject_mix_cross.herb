-- REJECT: & and | are different bitwise classes; mixing without parens is a
-- parse-time error (settled precedence c). Parenthesised form accepts.
func main():
    let inp = clogger()
    let a = index(inp, 0)
    return a & 2 | 3
end
