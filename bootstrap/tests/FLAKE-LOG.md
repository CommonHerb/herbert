# Kernel-arc gate FLAKE LOG — adjudicate CI/local REDs from evidence, not habit

**Purpose.** A standing record of *known* flake classes on the kernel-arc gates (`run_native_codegen_link17..66.sh`
+ `_mutation.sh`), so a RED is adjudicated against evidence: "is this a known re-rollable flake, or a real
miscompile?" A miscompile changes the emitted bytes (the seed sha moves, or a byte-pin/white-box gate fails). A
flake is a transient emulator/harness event on an **unchanged, byte-identical** kernel (the seed sha stays equal to
the live pin in `bootstrap/seed/gen1.seed.sha256` -- NEVER a literal copied into prose, which rots on every legitimate
reseed (blind-audit R2, 2026-08-29: two stale literals were found here); historical rows below quote the value current
at their date; QEMU-TCG/KVM/Bochs disagree on a run that later passes clean). **When in doubt, re-roll once and
compare** — a real bug reproduces deterministically; a flake does not.

**How to use.** On a CI/local RED: (1) find the failing step + signature; (2) match it below; (3) if it's a known
flake class on an untouched gate, re-run (record the outcome); (4) if it does NOT match, or reproduces
deterministically, treat it as real and investigate before landing/declaring green. Append new classes here.

---

## Known flake classes (as of 2026-09-04)

| # | class | gate(s) | signature | adjudication | status |
|---|---|---|---|---|---|
| F1 | **chasemap degeneracy** (mutation output-invisible on a degenerate random map) | `link53` (platter) `_mutation` M-fixedlba | `M-fixedlba GREEN on the chase (vacuous)` ~1 run in 64 | the random author-unknown chasemap self-loops at START (`cm[START]&0x3F==START`, p=1/64) or otherwise collapses the honest chase to `[cm[START]]×K`, so M-fixedlba is output-identical → false-RED. NOT a kernel bug. | **FIXED** — herbert `68c69ad` (map gen rejects+regenerates degenerate maps in both gate + mutation). |
| F2 | **Bochs feed-log / no-output harness** (a COM1 feeder that never LISTENs, or a Bochs run that produces no output, cascades to a wrong on-disk state → mis-attributed as a kernel RED) | `link60` (tract); **same class present, unhardened**, in the other multi-boot Bochs legs `link54`/`link55`/`link58`/`link59` and the single-boot Bochs feed legs `link44/45/48-52/56/57/61`; **observed on `link33` (lodger — HOST-side Bochs loop-mount race); link33 HARDENED 2026-08-22 (discriminator-sweep tranche 1b)** | e.g. `(C-Bochs) … RED … N0 not found in dir` while QEMU+KVM are GREEN in the SAME run | boot-1's feeder never bound its socket (feed*.log has no `LISTENING`) or Bochs produced no boot, so the kernel never got its late-bound input; the wrong FS state is a harness failure, not a miscompile. | **link60 HARDENED + COMPLETED** — herbert `3e2b450` (the SENT / feeder-delivered half) + the 2026-07-04 F2 sweep (COMPLETING the pattern): detects the harness failure (feeder never reached LISTENING, feeder never delivered its payload `SENT` — i.e. Bochs never connected COM1 — Bochs produced no output, **the boot never reached the kernel's `shutdown()` tail — `'shutdown requested'` absent — a boot killed or hung mid-run**, or **the GRUB config swap silently failed so Bochs booted the WRONG/stale module** (cross-model Codex); names the offending file), retries the 3-boot ×3 on a fresh disk, and grades reuseok as a kernel verdict ONLY once every boot LISTENED + delivered (SENT) + ran THROUGH `shutdown()`; 3 persistent harness failures → a loud, greppable `HARNESS-ERROR` marker (NOT the `FAIL: …` kernel-RED prefix), fail-closed only under REQUIRE_EMU=1, and the system-wide `pkill bochs` is scoped to this gate's own process. **Siblings: the F2 sweep (2026-07-04) is COMPLETE — link60's completed pattern is replicated across ALL F2-class siblings: link59, link55, link54, link58, link44, link45, link48, link49, link50, link51, link52, link56, link57, link61 (each one gate per commit, cross-model Codex-reviewed, full gate green locally under REQUIRE_EMU=1). A `(C-Bochs) …` RED means the HARNESS COMPLETED (NOT, on its own, a proven genuine kernel grade — parley/attest correction 2026-07-06/07: feeder-side SENT does NOT prove guest RECEIPT; a lost/late COM1 byte can misframe the input, so a lone RED may still be a capture-class flake — re-derive per the parley `57262f7` same-input replay discriminator, not the SENT+shutdown completion alone) — every Bochs boot LISTENED + delivered its payload (SENT) + ran THROUGH the kernel's `shutdown()` tail before the disk/emit was graded (link49's RUN-1 park test excepted: it withholds the byte by design, so only LISTENING applies there; link56/57/61 witness legs additionally distinguish a persistent struct-flake WITH a clean harness as a real trace-RED, not a re-roll). A harness/emulator failure now re-rolls ×3 and, if persistent, emits a distinct `HARNESS-ERROR` marker (never the `FAIL: …` kernel-RED prefix), fail-closed only under REQUIRE_EMU=1; the system-wide `pkill bochs` in every gate that had one is scoped to its own process.** **link33 observation (2026-07-17): kernel-codegen-l1 run `29594506346` attempt 1 RED at the L17/lodger step — `wrong fs type ... /dev/loop0p1` -> stale disk -> `shutdown=0` (a HOST-side loop-mount race feeding Bochs a stale/unmounted disk), on a gate the diff under test (f5cc7cb, L21/L23) never touched. FLAKE CONFIRMED — attempt 2 = success: `"attempt":2,"conclusion":"success"` on `headSha f5cc7cbdfbada704afe34555e784a154a66a2ef3` (finished `2026-07-17T18:49:55Z`); job `l1-dual-substrate | conclusion: success | run_attempt: 2`, `total steps: 104 | non-success/non-skipped: 0`; the L17 gate + L17 mutation steps both `success` (own gh polls, 2026-08-21). link33's Bochs harness-hardening stayed the named 1b residual until PAID 2026-08-22 (tranche 1b): every disk-build command CHECKED with explicit mount/loop cleanup on all exit paths (umount retry + never rm-over-a-live-mount; no global `losetup -D`), per-attempt CLASSIFICATION (DISK-BUILD(step)/EXTRACT-FAILURE/NO-OUTPUT/NO-SHUTDOWN vs COMPLETED), fresh-disk re-roll ×3, exhaustion -> greppable `HARNESS-ERROR` + UNCONDITIONAL fail-closed exit 1 (the tranche-1a-endorsed tightening — stricter than the older siblings' REQUIRE_EMU=1-only line), and only a boot that ran THROUGH `shutdown requested` is graded as a kernel verdict. Proven by a 10-row PATH-shim bite matrix (transient-mount re-roll then genuine adjudication; persistent mount/NO-OUTPUT/NO-SHUTDOWN/EXTRACT-FAILURE/bios-missing each exhaust fail-closed with ZERO `FAIL:` kernel-RED lines under REQUIRE_EMU=0 AND =1; COMPLETED+corrupt stream still a scored kernel RED; the OLD script mis-scored the same NO-SHUTDOWN stimulus as 6 kernel REDs; clean QEMU-only path stdout+stderr byte-identical old vs new) + cross-model Codex (2 full reviews -> 3 changes adopted -> intermittent-hang semantics adjudicated `RESOLVED-LAND` as the ratified fleet doctrine). FLEET SWEEP COMPLETE 2026-08-28 (Ben-greenlit): all 21 pre-link44 raw Bochs gates (link17-31, 34, 35, 40-43) rewired onto the SHARED `bootstrap/tests/bochs_f2_harness.sh` (checked disk build + cleanup guards incl. loop-attachment; DISK-BUILD(step)/NO-OUTPUT/NO-SHUTDOWN/EXTRACT-FAILURE — feed gates additionally FEED-NO-LISTEN/FEED-NO-SENT with LISTENING+SENT completion witnesses, feeder launched AFTER the disk build per the link31 accept-hold lesson — vs COMPLETED; fresh-disk re-roll ×3; exhaustion -> HARNESS-ERROR + UNCONDITIONAL fail-closed; only shutdown-complete boots graded). Verified per gate by a 6-7 row shim battery incl. a PLUMBING-AUDIT row (recorded cp/tee traffic must match the intended disk contents — added after cross-model Codex caught two generator cfg-argument bugs the shims were structurally blind to; both fixed, re-proven on all 21). Cross-model: Codex leg-1 LAND-WITH-CHANGES(5) -> adopted/reconciled; leg-2 BLOCK -> blocker fixed -> CONFIRM-LAND. REMAINING members of this class (queued, Ben's brief): the post-44 raw gates link46/47/53/62/65, AND the link44-61 F2-sweep gates' own unchecked disk builds + their legacy REQUIRE_EMU=1-only exhaustion (pre-1a semantics).** **CENSUS CORRECTION (2026-08-29): the 2026-08-28 sweep's "all 21 pre-link44 raw Bochs gates" claim was WRONG -- its census missed link32 (cloggard), link36 (sitopia) and link38 (coalgate), and left link37/link39 to tranche 1b. OBSERVED on link36: kernel-codegen-l1 run `33217378829` attempt 2 (the docs-only `62b6fcc` re-roll) RED at the L20 step -- `mount: /tmp/tmp.xUUABxbrhR/sit_echo.b.echo.197/mnt: wrong fs type, bad option, bad superblock on /dev/loop0p1` -> `umount: mnt: not mounted.` -> `FAIL: stack/native_compile_fragment.herb (sit_echo Bochs echo byte=197 (shutdown=0` / `0): RED   - no OWN table parsed (faulted before dump or RED) )` (the host-side loop-mount race, the `|| echo 0` -> `0\n0` non-numeric shutdown count, on a byte-identical, untouched kernel; job `l1-dual-substrate` id 99055432495, 2026-08-29T05:29:35Z-05:32:24Z). PAID same day: link32/36/38 rewired onto the shared harness one gate per commit (herbert `93cf2d9` / `b7a0999` / `9b6f32f`; per-gate PATH-shim battery on real Bochs boots: clean-path stdout+stderr identical old vs new; NO-SHUTDOWN-always -> `HARNESS-ERROR` with zero `FAIL:` lines under REQUIRE_EMU=1 (and =0 on link36); the OLD gates on the same stimulus mis-scored 4/2/2 kernel REDs (`... shutdown=0`); completed+corrupted-answer still a scored kernel RED; NO-SHUTDOWN-once -> one re-roll then PASS), cross-model Codex batched leg over the three. link37 (geeking: benign/victim/generic) + link39 (ouroboros: benign) went onto the harness via the tranche-1b REPLAY variants (herbert `2105efe`: `f2_bochs_feed_leg_replay` -- the same harness classes + the parley same-input replay discriminator on the Bochs substrate). The pre-link44 raw class is now CLOSED: 21 (sweep) + link33 (own hardening) + link32/36/38 (census correction) + link37/39 (1b replay). **GREEN-CONFIRMED (2026-08-29): run `33239363801` on `0a591c7` = `"attempt":1,"conclusion":"success"`, 104 steps / 0 non-success; the rewired gates' own live legs on the pinned pair (qemu 10.2.1 + bochs 2.8): `step 35 | success | L16 dual-substrate gate (cloggard ...)`, `step 43 | success | L20 dual-substrate gate (sitopia ...)`, `step 45 | success | L21 dual-substrate gate (geeking ...)`, `step 47 | success | L22 dual-substrate gate (coalgate ...)`, `step 49 | success | L23 dual-substrate gate (ouroboros ...)` — first attempt, no re-roll. CENSUS TOOL (the claim's proof, not a shared-file grep — link33 hardens INLINE): a gate is UNHARDENED iff it boots Bochs itself (`grep -q xvfb-run`) AND carries neither the shared harness nor an inline F2 block (`! grep -q 'bochs_f2_harness\|HARNESS-ERROR'`); over `run_native_codegen_link{17..43}.sh` that predicate now matches NOTHING; over 17..65 the remaining raw members are exactly link46/47/53/62/65 (brief 2).** |
| F3 | **geeking QEMU abnormal-exit** | `link37` (L21, geeking) gate | `geek_local echo byte=60: exit rc=245 != host_qemu_exit(T=60)=27` (kernel-codegen-l1 run `28689240315`, 2026-07-04) | QEMU exited abnormally (245, not the isa-debug-exit code 27) on an untouched, historically-green kernel; seed sha stable `8d08fe53…`; the parent's cold local sweep had it green. A QEMU runtime flake, not a miscompile. | **FLAKE CONFIRMED** — geeking PASSED clean on run `28691692998` (HEAD 3e2b450, 2026-07-04): the L21 gate + L21 mutation (42 checks) + the whole l1-dual-substrate job all GREEN. The `rc=245` was a transient QEMU abnormal-exit, not a miscompile (the geeking kernel is byte-identical between the failed and passing runs). (The direct re-run of `28689240315` was cancelled by a subsequent push's concurrency; adjudication moved to the equivalent-kernel `28691692998`.) |
| F4 | **Bochs boot-timeout / wall-clock** (a loaded runner makes a correct-but-slow Bochs boot time out → no completion) | any Bochs leg; observed `link22`/`link23` locally under concurrent load | `<probe> Bochs: frames(de<b>ad)=0 shutdown-evidence=0` (0 frames, 0 shutdown) | Bochs did not finish the boot in time (no wrong byte — just no completion); observed only under concurrent local load (a codex process + git running during a full `make kernel-verify`). Not a miscompile. | Mitigation: run `make kernel-verify` **clean** (nothing else consuming CPU); re-roll. The KVM real-silicon leg (fast, reliable) is the CI-uncoverable local anchor; the full 17..65 QEMU+Bochs is CI's job. |
| F5 | **cairn two-boot COM1** | `link55` (cairn) `_mutation` control | `control kernel is NOT clean (TARGET GREEN=0; DECOY GREEN=1 …)` | the control (unmutated) two-boot two-query cairn probe's TARGET query did not grade green on one run (a late-bound COM1 two-boot timing event) while the DECOY did; observed under load. Not a miscompile (cairn is byte-identical + CI-green). | **link55 Bochs leg HARDENED** by the F2 sweep (2026-07-04): the two-boot Bochs path now requires each boot to LISTEN + deliver (SENT) + swap-GRUB cleanly + run THROUGH `shutdown()`, and re-rolls ×3 on a harness failure -- so a COM1-timing event on the Bochs leg re-rolls instead of false-REDding. (The `_mutation` control's own two-boot COM1 timing is a separate harness; re-roll it per this class until it too is swept.) **OBSERVED on the MAIN link55 gate's (C-HOSTILE-DF) leg (2026-08-28): kernel-codegen-l1 run `33217378829` attempt 1 (docs-only `62b6fcc`, VERIFYING.md only) RED at the L39 step -- `FAIL: stack/native_compile_fragment.herb ((C-HOSTILE-DF) the genuine kernel mis-resolved under a hostile std=DF=1 (emitted=, want=11455e0325e4ed2be78605832d62e69d26d3ca5778f2a9c6a89d59bce3944a1d0846f5566e13e38fafff0afbb8) ...)` / `native-codegen link55 (cairn / FILESYSTEM GERM -- a PERSISTENT NAMED LOOKUP): pass=22 fail=1` (job 99003919748, step 81, 23:43:32Z-23:49:44Z), with `PASS: (B1) cairn kernel byte-identical to cairn_ref.build_elf() [33900 B]` in the same run and every other cairn leg GREEN. Signature = an EMPTY emission on a byte-identical kernel: the leg's `boot_feed_emit` retries BOOT-2 (the getter) x4 but never BOOT-1 (the putter) and has no same-seed replay, so a lost/late BOOT-1 COM1 put byte (records never landed / misframed -> found=0 -> empty) reproduces on every BOOT-2 retry -- the transport-reachable shape the parley comment names (`EMPTY *and* WRONG bytes are both transport-reachable here`). Quiet-host local repro on kingdom (qemu 10.2.1, REQUIRE_EMU=1, 2026-08-29T05:08-05:14Z): `native-codegen link55 (cairn / FILESYSTEM GERM -- a PERSISTENT NAMED LOOKUP): pass=24 fail=0` incl. `PASS: (C-HOSTILE-DF) a SYS_FS_GET preceded by a hostile std (DF=1) of a VALID name STILL resolves correctly ...`. Attempt 2 never reached L39 (RED at L20 -- the link36 F2 observation above). **FLAKE CONFIRMED (2026-08-29):** kernel-codegen-l1 run `33239363801` on `0a591c7` (the tranche-1b/census landing; cairn kernel + link55 gate byte-identical, untouched by the diff) = `"attempt":1,"conclusion":"success"`; job `l1-dual-substrate` id 99065921082 `conclusion: success`, `total: 104, nonsuccess: 0`; `step 81 | success | 2026-08-29T07:47:40Z -> 2026-08-29T07:53:00Z | L39 dual-substrate gate (cairn ...)` and `step 82 | success | ... | L39 mutation proof` (own `gh run view --json jobs` poll). The attempt-1 empty emission did not recur on the same bytes: a one-shot BOOT-1 COM1 transport miss, not a miscompile. NAMED RESIDUAL (discriminator-half, charter): the main gate's hostile legs (C-HOSTILE-LBA/CARRY/DF) still lack BOOT-1 retry + the same-seed replay the C-QEMU/C-Bochs legs have -- a follow-up, not silently absorbed. **RESIDUAL PAID 2026-09-04** (`MEWTWO/audits/f5-residual-2026-09-04/`): the two hostile legs that HAVE a BOOT-1 (`C-HOSTILE-CARRY`, `C-HOSTILE-DF`) now carry a guest-side BOOT-1 receipt (the putter echoes back, by SYS_WRITE, the exact bytes it read over COM1) AND a host-side storage read-back that OUTRANKS it; `C-HOSTILE-LBA` has NO BOOT-1 at all -- it is a crafted host disk plus one getter boot -- and what it gained instead is the frame-COUNT liveness receipt (the dir entries + payload sectors read off the image, because an echo proves RECEIPT and not STORAGE and the medium is the only thing BOOT-2 reads), a bounded re-roll that separates an ABSENT receipt (transport, re-rolled) from a WRONG one (adjudicated, never re-rolled), ONE same-seed replay each, a frame-COUNT liveness receipt where the expected result is an empty emission, a wall-clock ceiling, and twelve boot-free positive controls. RED-first both ways on silicon: a forced lost BOOT-1 byte is detected AT BOOT-1 and re-rolled (`FRAMES=2 want=4`, then `BOOT-1 tries=2: OK`), exhaustion is two `HARNESS-ERROR` lines with ZERO kernel-RED lines, and each mutant (`M-fsnocld`/`M-nolbabound`/`M-nocarrycheck`) stays a scored kernel RED. **BUT THE CLASS'S CAUSE IS NOT A TRANSPORT MISS -- SEE F11.** F5's signature was reproduced ON DEMAND, both engines, on the genuine byte-pinned kernel, by choosing a seed whose query carries a 0xD4 within three bytes of its end, with the transport PROVABLY CLEAN. The three adjudications above ('a one-shot BOOT-1 COM1 transport miss') were therefore made against the wrong mechanism; they are left standing as the record of what was believed at the time.** |
| F6 | **watchdog-kill on abnormal QEMU exit** (a benign module scored watchdog-KILLED when QEMU exits abnormally — F3's rc=245 shape surfacing through a kill-witness leg) | frozen links in the CI battery; observed on kernel-codegen-l1 run `29163654990` attempt 1 (the gyre land, 2026-07-11) | `add7 byte=60: exit rc=245 … grade: RED … benign module was KILLED by the watchdog (kill-witness present)` on a link NOT touched by the diff under test | the kernel is byte-identical (seed sha unchanged; the failing leg's link was outside the gyre diff); rc=245 is the QEMU abnormal-exit signature (F3), and a kill-witness on a benign module means the one-shot watchdog fired around an emulator stall — a substrate event, not a miscompile. | **FLAKE CONFIRMED** — the same run's attempt 2 = `completed/success` (`gh api`, re-polled + quoted in canon 2026-07-16). Adjudicate per F3 + the parley same-input replay discriminator; record every re-roll honestly (green on attempt 2 is never "first-attempt green"). |
| F7 | **load-correlated QEMU timeout** (parallel host load makes correct-but-slow QEMU legs time out — F4's failure shape on the QEMU side, load-correlated) | any QEMU leg; observed `link55` (parent cold sweep, 2026-07-16: RED under parallel load, clean twice when quiet) | a QEMU leg times out or completes with no output while the host carries concurrent CPU load; the identical gate is clean on a quiet host | environmental and load-CORRELATED — which means the parley same-input replay discriminator canNOT separate it (under sustained load the replay fails the same environmental way, so a harness event can masquerade as a reproduced hard RED). Not a miscompile. | Mitigation (doctrine): **never grade kernel gates on a saturated host** — run `make kernel-verify` with nothing else consuming CPU; a RED observed under load is re-adjudicated on a quiet host before being treated as real. |
| F8 | **QEMU-VERSION-specific guest SIGABRT — NOT a re-rollable flake** (a pinned CI QEMU build aborts on a specific guest that a newer QEMU runs clean) | `link34` (trikonderoga) `_mutation` on GitHub CI (`kernel-codegen-l1`, QEMU **8.2.2**-pinned); run `29548360981` attempts 1/2/3 (2026-07-16) all identical | `run_native_codegen_link34_mutation.sh: line 45: NNNNN Aborted  timeout 60 qemu-system-x86_64 ...` (SIGABRT, empty debugcon) → the fail-closed remediation's `FAIL: link34 HARNESS FAILURE -- a QEMU run was dead/timed-out (empty output); fail-closed, NOT a genuine pass` | **DETERMINISTIC on CI's pinned QEMU 8.2.2 (3/3 identical), so re-rolling does NOT clear it** — distinct from F3/F6 (random abnormal-exit) and F4/F7 (load timeout). Local QEMU **10.2.1** runs link34 mutation **3/3 GREEN** the CI way + the full suite GREEN, so it is a QEMU-8.2.2-specific guest abort, NOT a miscompile (byte-pins hold, seed sha stable). The gate-fail-closed remediation is behaving correctly — it refuses to certify the dead run that the old fail-open harness scored empty→RED→fake-"bite"→PASS. | **Do NOT re-roll and do NOT revert the remediation** (reverting re-installs the 45-harness fail-open). Fix is a DELIBERATE substrate change: bump the CI QEMU pin toward 10.2.x (full suite already golden-validated on 10.2.1 locally) after re-validating per the workflow's pin-change comment, OR diagnose the aborting leg on an 8.2.2 install. Queued verdict-first for Ben (2026-07-16); Ben RATIFIED the pin bump 2026-07-16. **LANDED 2026-07-17 (herbert `2494945`)**: runner `ubuntu-24.04` -> `ubuntu-26.04` (PREVIEW image, deliberate — asserts fail closed on emulator-version churn), pins qemu `1:10.2.1+ds-1ubuntu3.1` + bochs `2.8+dfsg-1` — the exact qemu+bochs pair the full suite is golden-validated on locally. **GREEN-CONFIRMED: kernel-codegen-l1 run `29558323070` = `completed/success` on `2494945c…` (attempt 1, finished `2026-07-17T08:16:42Z`; job `l1-dual-substrate` 104 steps ZERO non-success; pin-assert `success`; L18/trikonderoga gate + mutation both `success` — own gh polls).** |
| F9 | **SELF-INFLICTED host load — a gate that ORPHANS its own emulators** (the F7 condition manufactured BY the gate under test, not by a neighbour) | `link66_mutation` while it was being authored (2026-09-03); **the shape is generic to any gate that backgrounds `timeout N qemu … &` and later kills `$!`** | no gate line at all — the symptom is on the HOST: `qemu-system-x86_64` processes that outlive their run, each pinning ~83% of a core, and a 1-minute load average that climbs run over run. Observed: **24 orphans, the oldest 50 minutes old, load average 0.4 → 27**, and the gate's own wall time 1m41 → 4m04 as it starved itself | **NOT a flake and NOT a miscompile — a harness bug with flake CONSEQUENCES.** `timeout N cmd &` makes `$!` the **`timeout` wrapper**, not the emulator; killing the wrapper (e.g. to end a session whose verdict is already recorded) leaves the emulator running with nothing left to time it out. Every such kill leaks one spinning emulator. The leaked load then reproduces F7/F4 on the NEXT run — on a host whose quiet is the precondition for grading — so a real timeout and a self-inflicted one become indistinguishable. **Adjudicate by counting processes, not by reading logs.** | **FIXED in `link66_mutation` (2026-09-03)**: the emulator IS the background job (so `kill -9 "$qp"` reaches it) and the wall-clock ceiling is a SEPARATE watchdog that leaves a marker file, so a watchdog kill can never be read as the harness's own deliberate end-of-session. Verified `qemu before: 0 … qemu after: 0` across a full run, and the wall time returned to 1m41. **Two standing lessons: (i) before grading, check the emulator count with `ps -eo comm \| grep -cx 'qemu-system-x86'` — NOT `pgrep -fc qemu-system-x86_64`, which matches the checking shell's own command line and reported 2 where the truth was 0; (ii) a gate that leaks processes poisons every later run's quiet-host precondition, so this belongs in the same conversation as F4/F7 rather than in a private note.** |
| F10 | **mawk-versus-gawk divergence — NOT a flake, a PORTABILITY bug that presents as a gate failure** | any gate or proof whose shell uses `awk` with a REGEX; observed on `link66_mutation` (CI run `33856459556` step 104, 2026-09-04) | `awk: warning: escape sequence \`\$' treated as plain \`$'` followed by the gate's own fail-closed line, here `FAIL: link66-mutation (extract the forcing source: empty)` — a 0-second failure, no emulator involved | **DETERMINISTIC per host, so re-rolling never clears it, and it is NOT a miscompile.** kingdom's `/usr/bin/awk` is **mawk**; CI's is **gawk**. They disagree on backslash escapes inside regexes: mawk reads `\$` as a literal dollar, gawk reads it as plain `$` — the END ANCHOR — so a pattern written to match the text `$tmp` matches locally and can never match on CI. The same class covers open-ended intervals (`{n,}`, which mawk mis-handles — see the kingdom scorecard bug this project already worked around) and any other escape whose meaning is dialect-dependent. **A gate that passes locally and fails at 0 s on CI with an awk warning is this, not the kernel.** | **CLOSED for link66 (2026-09-04):** the mutation proof's ten extractions and all of its uniqueness guards were rewritten to use **fixed-string** matching in python (`eq:` / `sw:` / `ew:`), and the script **invokes no awk at all**; the lift anchors and the helper are regex-free, and the extracted text is proven byte-identical to the known-good mawk output on all ten subjects. **The claim is deliberately narrow, after a review leg found a broader one false:** ordinary regexes remain elsewhere in the file (python `re`, a few `grep` patterns) and they are fine — what is banned is a dialect-sensitive construct in an `awk` program. `gawk` is NOT installed on kingdom, so the replacement could not be run against it — which is precisely why the fix removes the dialect dependence instead of patching the one escape. **STANDING RULE: do not put a DIALECT-SENSITIVE construct in an `awk` program in a gate** — no backslash escapes inside a regex (`\$`, `\(`, `\{`), no open-ended intervals (`{n,}`). Plain POSIX regexes and field access (`{print $2}`) are portable and were audited as such; the safest course, and the one taken here, is fixed strings in `grep -F`, `sed`, or python. |
| F11 | **THE DEBUGCON FRAME PARSER IS NOT BINARY-SAFE — an OPEN, NAMED DEBT, *NOT* a re-rollable flake** | every gate graded through `_wframes` in the **nineteen** oracles `lethe cairn rollcall durable tickover cleave furlough tessera homestead platter tandem tenement delete backfill larder larder_step0 tract growheap highwater` (`*_ref.py`), **plus `holler_ref.py`'s whole-stream `_wframe`** serving the four confused-deputy LEAK graders, plus the named sibling scanners (`_commit_frames`, `_wake_frames`, `_disp_counts`, `_cow_frames`, `_rframes`) and dozens of `re.search(rb'\xNN…')` scans over the tail | **BOTH directions, and neither reproduces on a re-roll with a NEW seed while BOTH reproduce on the SAME seed.** FALSE RED: an `emitted=` empty emission on a byte-identical kernel (this is **F5's entire signature**). FALSE GREEN: a genuinely LEAKING kernel graded as the clean reject, because the frame that gets hidden is the one carrying the leak | **A HOST-SIDE GRADING DEFECT, deterministic per seed.** `_wframes` scans the whole trace for the write-frame opener `0xD4` and then TRUSTS a length field read at that offset. `do_read_ready` (`cairn_ref.py:606-613`) echoes **THE RAW BYTE JUST READ** into its own `0xC0` record, so a late-bound COM1 byte equal to `0xD4` is read as a frame header; `ln` takes the cs dump's value `0x1b`=27 and the scan advances `18+27`=45 bytes — exactly three read-records — stepping clean over the genuine answer frame. **~1.17% per graded emission** (`1-(255/256)^3 = 0.011673`, exact). **The per-RUN figure is an ESTIMATE, not a derivation:** ~11% assumes ~10 graded emissions in a link55 run, a count taken by inspection and not proven; the F5 residual's own BOOT-1 receipt ADDS four more emitted frames per BOOT-1, so the true per-run exposure is higher than 11%, not lower. Treat 1.17%/emission as the load-bearing number. Because it is a function of the SEED and not of timing, a same-seed replay REPRODUCES it, so every replay discriminator in the tree — including the one F5's residual added — calls it *"a deterministic same-input kernel failure"*. **Re-rolling with a fresh seed USUALLY clears it -- about 98.8% of the time per emission, NOT always -- which is exactly why it was adjudicated as a flake three times.** Two independently drawn seeds that do NOT clear: `78061b1fe2c3175b` and `ea9bb6f2d93a16e1` both put a `0xD4` in the vulnerable last-three-byte window. A re-roll that happens to pass is evidence of nothing. | **OPEN. Owed to `PACKET-DEBUGCON-PARSER-R2` slice B** (a fresh seat, design-first). Record, with a re-runnable proof (`wframes-binary-unsafety.py`) and an on-silicon reproduction on both engines: `MEWTWO/audits/f5-residual-2026-09-04/`. **ATTEMPT 1 WAS WRITTEN AND REVERTED** — it returned no frames on seven of nineteen oracles' real traces and four graders turned that into GREEN, so it was strictly worse than the defect; the verdicts, the three methodological failures and the re-charter spec are in `MEWTWO/audits/debugcon-parser-2026-09-04/REPORT.md`. **KNOWN IN-REPO SINCE 2026-06-14 and never logged until now:** `chiefturbo_ref.py:344` (*"a spurious 0xD4 inside an address field … the way H._all_wframes (which trusts the len) would"*), `mmj_ref.py:265` (*"the critic's nice-to-have. The chosen probes (N<=20) never trigger it"*), and `cleave_ref.py:1595` / `furlough_ref.py:1434` / `tessera_ref.py:1535` (*"a spurious 0xD4 byte in **the boot cell-dump** cannot derail the write-frame scan"* — which checked the pre-table region and stopped one region short of the read records in the tail). **ADJUDICATION RULE UNTIL SLICE B LANDS: an `emitted=`-empty RED on a late-bound gate is NOT automatically an F5 transport flake. Print and record the seed (every seed-drawing gate now prints `  SEED <var>=<hex>`), and re-run on the SAME seed. Same-seed recurrence + a clean transport receipt is THIS class, not a kernel RED.** The false-GREEN direction is a REALNESS finding about the landed arc and is recorded as such in `STATE-OF-PLAY.md`. |

---

## The invariant that separates flake from bug

- **Real miscompile:** the emitted bytes changed. Symptoms: the gen-1 seed sha moves off the value pinned in
  `bootstrap/seed/gen1.seed.sha256` (the michoi seed gate in `make test` goes RED -- read the file, not this prose); a full-image
  byte-pin (`… image != committed golden`) fails; a white-box `assert_*` fails; a mutation FAILS TO BITE
  *deterministically* across re-rolls. Investigate — do NOT re-roll away.
- **Flake:** the kernel is byte-identical (seed sha stable, byte-pins pass) and the disagreement is a substrate/harness
  event that does not reproduce. Re-roll and record.
